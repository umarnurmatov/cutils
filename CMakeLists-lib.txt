# cmake_minimum_required(VERSION 3.10)

# set(TARGET_LIB_NAME cutils)

# file(GLOB_RECURSE source_files ${CMAKE_CURRENT_SOURCE_DIR}/src/*.c)


# add_library(${TARGET_LIB_NAME} ${source_files})
# target_include_directories(${TARGET_LIB_NAME} INTERFACE ${CMAKE_CURRENT_SOURCE_DIR}/include)


